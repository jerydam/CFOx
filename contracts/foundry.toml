[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.30"
evm_version = "cancun"
via_ir = true
optimizer = true
optimizer_runs = 200
verbosity = 5
[profile.default.fuzz]
runs = 1000

[rpc_endpoints]
celo = "${CELO_RPC_URL}"
celo_sepolia = "https://forno.celo-sepolia.celo-testnet.org"
botchain = "${BOTCHAIN_RPC_URL}"

[etherscan]
# Blockscout doesn't need an API key — "any" satisfies Foundry's non-empty requirement
celo_sepolia = { key = "any", url = "https://celo-sepolia.blockscout.com/api/" }
celo = { key = "${CELOSCAN_API_KEY}", url = "https://api.celoscan.io/api" }
